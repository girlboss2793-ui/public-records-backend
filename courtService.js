// courtService.js
module.exports = {
  lookup: async (name, state) => {
    // Placeholder until we add real API calls
    return [
      {
        source: "courtlistener",
        case: "No real data yet",
        nameSearched: name,
        stateSearched: state
      }
    ];
  }
};